export enum Environment {
  Development = 'development',
  Production = 'production',
  Test = 'test'
}

export enum ActivityType {
  Call = 'call',
  Email = 'email',
  Meeting = 'meeting',
  Note = 'note',
  Task = 'task'
}
